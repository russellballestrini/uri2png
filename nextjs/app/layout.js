export const metadata = {
  title: "uri2png",
  description: "Screenshot any URI via a permacomputer",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
